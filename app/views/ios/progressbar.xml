<Alloy>
	<Window title="Progress Bar">
		<ScrollView>
			<View class="vbox">>

				<Label class="help">You could already set the color of the message (color:blue) and the completed part of the Progress Bar (tintColor:green). Now you also set the portion of the bar that is NOT filled (trackTintColor:red).</Label>
				<ProgressBar class="tpad-s" message="[message]" />

			</View>
		</ScrollView>
	</Window>
</Alloy>
